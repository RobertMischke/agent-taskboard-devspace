The scaffold is in place from the previous task. Now add the visible content to `index.html`.

Inside `<main>`, add exactly these elements in this order:

- An `<h1>` with the text `Click Counter`.
- A `<p>` paragraph: `Press the button to increment the counter.`
- A `<button id="increment">+1</button>` (no JavaScript yet, that is the next task).
- A `<p>Current count: <span id="count">0</span></p>`.

Edit only `index.html`. Do not touch `style.css`, `script.js`, or `README.md`. The next task is responsible for the JavaScript wiring; do not pre-empt it.

Keep your reply short. One file edit, no exploration.
