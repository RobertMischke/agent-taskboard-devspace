The page works but it looks unstyled. Add basic CSS to `style.css` so the page reads as "intentional" rather than browser-default.

Edit only `style.css`. Do not modify `index.html`, `script.js`, or `README.md`.

The goal is open: "looks clean and intentional". Beyond that, choose your own direction:

- typography (font family, size, line-height)
- color theme (light, dark, accent for the button)
- layout (centered max-width, spacing around elements)
- button styling (radius, padding, hover state)
- whatever else feels right

Keep the CSS short, under 60 lines. No frameworks, no `@import`, no preprocessor syntax.

This task is **intentionally ambiguous**. It is the test scenario's steering checkpoint: the user may send a Steer-mode follow-up after this run lands in review, redirecting your styling toward something specific (for example "use a dark Catppuccin palette, button radius 6px"). Do your best with the open spec above first; the user trusts you to make a defensible call. If a Steer follow-up arrives, treat it as authoritative and override your earlier choices in `style.css`.

When you finish, end with `[[TASK_DONE]]` (or `[[TASK_BLOCKED:<reason>]]` only if the file is genuinely impossible to write).
