Create the file scaffold for a tiny click-counter web page. No logic yet, just empty / placeholder files so the next task can fill them.

Create exactly these files in the working directory:

- `index.html` with a standard HTML5 boilerplate (`<!doctype html>`, `<html lang="en">`, `<head>` with `<meta charset="utf-8">`, `<title>Click Counter</title>`, `<link rel="stylesheet" href="style.css">`, `<body>` with a single empty `<main></main>` and `<script src="script.js"></script>` placed before `</body>`).
- `style.css` containing only the comment `/* click-counter scenario */`.
- `script.js` containing only the comment `// click-counter scenario`.
- `README.md` with the heading `# Click Counter` and a one-sentence summary.

Do not add any logic, styles, or button markup yet. Subsequent tasks fill those in.

Keep your reply short. The visible deliverable is the four files on disk, not your prose.
